/**
 * Compatibility exports for older imports. New code should import directly
 * from supabase-client and supabase-config.
 */
export { createClient } from './supabase-client';
export { isSupabaseConfigured } from './supabase-config';
